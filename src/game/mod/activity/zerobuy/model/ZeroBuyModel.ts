namespace game.mod.activity {

    import common_reward_status = msg.common_reward_status;

    export class ZeroBuyModel {
        public infos: common_reward_status[];

        public index: number = -1;
    }
}
